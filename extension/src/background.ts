// Service worker for the LeadVault Extension
chrome.runtime.onInstalled.addListener(() => {
  console.log("LeadVault Extension Installed successfully.");
});

// We can add other background tasks here in the future.
